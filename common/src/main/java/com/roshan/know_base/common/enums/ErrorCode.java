package com.roshan.know_base.common.enums;

public enum ErrorCode {
    NOT_FOUND,
    INVALID_REQUEST,
    UNAUTHORIZED,
    FORBIDDEN,
    ACCESS_DENIED,
    INVALID_ROLE,
    INVALID_CREDENTIAL,
    ALREADY_EXISTS
}
